# 🚀 AI 보안 가드레일 챗봇 — 타 컴퓨터(새 PC) 실행 가이드

본 프로젝트는 고가의 GPU 서버나 복잡한 환경 설정 없이도, **일반 PC나 노트북에서 즉시 구동**될 수 있도록 완전 독립형(Self-Contained)으로 설계되었습니다.

---

## 1. 사전 준비 (새 컴퓨터에서 최초 1회)

### ① 파이썬(Python) 설치
* **권장 버전**: Python 3.10 ~ 3.12
* **공식 다운로드**: [https://www.python.org/downloads/](https://www.python.org/downloads/)
* ⚠️ **주의사항 (매우 중요)**:  
  설치 마법사 첫 화면 맨 아래에 있는 **`[✓] Add python.exe to PATH` (환경변수 추가)** 체크박스를 반드시 체크하고 설치해 주세요!

---

## 2. 프로젝트 폴더 이동
* 현재 작업 중인 `ai-guardrail-chatbot` 폴더 전체(또는 `ai-guardrail-chatbot.zip` 압축 해제본)를 USB나 클라우드를 통해 다른 컴퓨터의 원하는 위치(예: 바탕화면)에 복사합니다.

---

## 3. 원클릭 실행 (가장 쉬운 방법)

1. `ai-guardrail-chatbot` 폴더를 엽니다.
2. **`run_demo.bat`** 파일을 마우스로 **더블클릭**합니다.
3. 스크립트가 자동으로 동작합니다:
   * 필수 패키지(`fastapi`, `streamlit` 등)가 없으면 `pip install -r requirements.txt`를 자동으로 설치합니다.
   * 백엔드 API 서버(포트: 8000)를 실행합니다.
   * 프론트엔드 대시보드(포트: 8501)를 실행합니다.
4. 웹 브라우저가 열리며 **`http://localhost:8501`** 챗봇 화면이 뜹니다!

---

## 4. 수동 터미널 실행 방법 (직접 실행 시)

만약 명령 프롬프트(cmd)나 PowerShell에서 직접 실행하고 싶다면:

### [터미널 1 - 백엔드 API 서버]
```bash
cd ai-guardrail-chatbot
pip install -r requirements.txt
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
```

### [터미널 2 - 프론트엔드 Streamlit 웹 UI]
```bash
cd ai-guardrail-chatbot
streamlit run frontend/app.py --server.port 8501
```

---

## 5. 접속 URL 안내

| 서비스 명칭 | 접속 주소 (URL) | 설명 |
|:---|:---|:---|
| 🛡️ **챗봇 웹 관제 대시보드** | **http://localhost:8501** | 가드레일 ON/OFF 토글, 채팅 UI, 차단 관제 패널 |
| 🔌 **FastAPI REST API 문서** | **http://localhost:8000/docs** | 백엔드 Swagger API 명세 및 헬스체크 |

---

## 6. 자주 묻는 질문 (FAQ)

### Q1. 다른 컴퓨터에 Ollama나 대규모 LLM 모델이 안 깔려 있어도 되나요?
* **네, 완벽하게 실행됩니다!**
* 백엔드에 **스마트 샌드백(Mock SLM) 모드**가 내장되어 있어, Ollama가 없어도 가드레일 ON/OFF 비교, 실시간 공격 차단(0.2ms), 주민번호/비밀번호 마스킹, Before/After 시연 등 모든 기능이 100% 정상 작동합니다.

### Q2. 실제 무검열 로컬 SLM(Qwen2.5) 모델까지 다른 PC에서 직접 돌리고 싶다면?
1. [Ollama 공식 홈페이지](https://ollama.com/)에서 Ollama 다운로드 및 설치
2. 터미널에서 `ollama run qwen2.5:7b-instruct-abliterated` 실행
3. 우리 챗봇의 `run_demo.bat`을 실행하면 백엔드가 자동으로 로컬 Ollama를 감지하여 실제 모델로 전환됩니다.
